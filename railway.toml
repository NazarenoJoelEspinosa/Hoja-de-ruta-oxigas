[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/api-server build"

[deploy]
startCommand = "node artifacts/api-server/dist/index.mjs"
restartPolicyType = "on_failure"
